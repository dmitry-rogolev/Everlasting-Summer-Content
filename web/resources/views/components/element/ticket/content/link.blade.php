<x-element.ticket.content class="border-{{ $theme }}-hover cursor-pointer {{ $class }}" style="{{ $style }}" image="{{ $image }}" href="{{ $href }}" {{ $attributes }}>
    {{ $slot }}
</x-element.ticket.content>